'use strict';

window.onload = () => {
  const elementCardFront = document.querySelector('#card-front');
  const elementCardBack = document.querySelector('#card-back');
  const elementButtonContact = document.querySelector('#button-contact');

  elementButtonContact.addEventListener('click', () => {});

  console.log(elementButtonContact);
};
